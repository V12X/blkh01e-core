# CI mínimo do núcleo: a suíte inteira (vetores RFC, fuzz, propriedades do ratchet) + varredura
# de segredos em TODO push/PR. Nasceu de auditoria (v12x-scan 2026-08-13): verificação que não
# roda sozinha regride. macOS é obrigatório (CryptoKit não existe em Linux).
name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

# Ações fixadas por SHA, não por tag: tag é ponteiro MÓVEL — quem controla o repositório da ação
# (ou quem o comprometer) pode reapontar `v4` para outro código, que roda aqui com acesso ao
# checkout. O comentário diz a versão legível; o SHA é o que manda.
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262   # v4.4.0
      - name: swift test
        run: swift test

  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262   # v4.4.0
        with:
          fetch-depth: 0   # histórico completo — segredo em commit antigo também é vazamento
      - name: gitleaks (histórico completo)
        run: |
          curl -sSfL -o gl.tgz https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          echo "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  gl.tgz" | sha256sum -c -   # checksum publicado na release; tarball trocado = build vermelho
          tar -xzf gl.tgz gitleaks
          ./gitleaks git . --redact
